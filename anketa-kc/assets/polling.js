/**
 * polling.js — модуль автообновления расписания
 *
 * В текущей версии автоматическое обновление расписания через setInterval ОТКЛЮЧЕНО.
 * Это сделано сознательно: частые API-запросы к Битрикс24 нагружают портал
 * и расходуют лимиты REST API.
 *
 * Расписание обновляется тремя способами:
 *   1. Автоматически при загрузке страницы — через initCalendar() в app.js
 *   2. Автоматически после бронирования   — через loadAllSlots() в calendar.js
 *   3. Вручную по кнопке «Обновить расписание» — через btn-refresh-slots в index.php
 *
 * Функция startPolling() оставлена как no-op (пустая заглушка) для совместимости:
 * она вызывается в app.js после initCalendar(), но ничего не делает.
 * Если в будущем потребуется автообновление — реализовать логику здесь.
 */

'use strict';

/**
 * startPolling — точка входа для автоматического обновления расписания.
 * Вызывается из app.js после инициализации календаря.
 * В текущей версии — пустая функция (no-op).
 */
function startPolling() {
  // Автообновление отключено.
  // Расписание обновляется вручную кнопкой «Обновить расписание»
  // или автоматически при загрузке и после бронирования.
}
