<?php
/**
 * config.php — конфигурация локального приложения «Анкета (МКЦ + МП)»
 *
 * Этот файл содержит все константы, используемые приложением.
 * Все вызовы REST API Битрикс24 выполняются на клиенте через BX24 JS SDK.
 * Вебхук хранится здесь на случай серверных вызовов в будущем.
 *
 * Константы передаются из PHP в JavaScript через index.php (window.APP_CONFIG).
 */

// URL портала Битрикс24 (используется для формирования ссылок и подключения BX24 JS SDK)
define('PORTAL_URL', 'crm.yurclick.com');

// Входящий вебхук Битрикс24 для серверных вызовов REST API
// Скоупы вебхука: bizproc, calendar, crm
// Не используется в текущей версии (всё через BX24 JS SDK), но сохранён для будущих нужд
define('WEBHOOK_URL', 'https://crm.yurclick.com/rest/6/m1umtpppnvj21gud/');

// ID шаблона бизнес-процесса «Назначить встречу» в Битрикс24
// Запускается из calendar.js при бронировании слота через bizproc.workflow.start
define('BP_TEMPLATE_ID', 40);

// ID отдела продаж в структуре компании Битрикс24
// Зарезервировано для будущей фильтрации сотрудников по отделу
define('SALES_DEPT_ID', 1);

// Длительность одного слота записи в минутах (только часовые слоты)
// Используется в calendar.js для расчёта свободных интервалов
define('SLOT_DURATION_MIN', 60);

// Сколько дней вперёд показывать слоты в расписании
// Используется в calendar.js: если на текущий день мало слотов — переход на следующий
define('SLOT_HORIZON_DAYS', 7);

// Минимальное количество свободных слотов на день
// Если слотов меньше — calendar.js автоматически переключает на следующий рабочий день
define('MIN_SLOTS_PER_DAY', 1);

// Начало «разумного» часа для клиента по его часовому поясу
// Слоты до этого часа (по TZ клиента) не показываются в расписании
define('CLIENT_HOUR_MIN', 9);

// Конец «разумного» часа для клиента по его часовому поясу
// Слоты после этого часа (по TZ клиента) не показываются в расписании
define('CLIENT_HOUR_MAX', 20);
